import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pestcontrol.app',
  appName: 'Control de Plagas',
  webDir: 'dist/public'
};

export default config;
